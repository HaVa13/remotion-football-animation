{
  "name": "remotion-football-animation",
  "version": "1.0.0",
  "description": "Animation of a boy playing football with big ball impact",
  "scripts": {
    "start": "remotion preview src/index.tsx",
    "build": "remotion render src/index.tsx --props '{}'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "remotion": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
